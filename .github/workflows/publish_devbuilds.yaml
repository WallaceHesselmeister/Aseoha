name: Automated donator builds

on:
  workflow_dispatch:
  push:
    paths:
      - 'src/**'
      - 'build.gradle'
      - 'gradle.properties'
      - 'settings.gradle'
      - 'gradle/**'
      - 'gradlew'
      - 'gradlew.bat'
      - 'versioning.gradle'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Run data generation
        run: ./gradlew runDatagen

      - name: Build with Gradle
        run: ./gradlew build
        # env:
        #   BETA: 1 # exclude assets if it's a beta dev build

      - name: Publish artifacts to Discord (test mode)
        uses: DrTheodor/discord-webhook-upload-action@v1.2
        with:
          # this url is gonna be removed in the next few hours so dont even try to use it
          # ${{ secrets.DEV_BUILDS }}
          url: https://discord.com/api/webhooks/1394828486807523530/Ys21olBf04jJHTgORj_H3C_fkeYkvbWXU3K-SvTXhrC0UsnZolWcd2BmA9IVDuB8mu-J
          file: 'build/libs/*'
          username: 'George Washington'
          avatar: 'https://i.imgur.com/uiFqrQh.png'
          commit: '> :sparkles: [%MESSAGE%](<%LINK%>) by [%AUTHOR%](<%AUTHOR_LINK%>)'
          message: |
            <:new1:1253371736510959636><:new2:1253371805734015006> New `All Seeing Eye Of Harmony Additions` dev build `#${{ github.run_number }}`:
            %COMMITS%
